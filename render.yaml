services:
  - type: web
    name: hostex-proxy
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: HOSTEX_TOKEN
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: CRON_SECRET
        sync: false
